#include <iostream>
using namespace std;


int main(){

    
//Q1 Declare an integer array of size 5 and initialize it with values 1, 2, 3, 4, 5.
int arr[5] = {1,2,3,4,5};
cout << arr << endl;    // it will print the memo add

// Print all elements of an array using a for loop.

for (int idx=0; idx<sizeof(arr)/sizeof(int); idx++){
    cout<< arr[idx] << endl;
}

// Reverse the elements of an array.

// Count how many even and odd numbers are in an array.

// Find the average of elements in an array.

// Level 2: Intermediate

// Check if a given element exists in an array.

// Find the first occurrence index of a given element.

// Find the last occurrence index of a given element.

// Sort an array in ascending order using bubble sort.

// Sort an array in descending order using selection sort.

// Rotate an array left by one position.

// Rotate an array right by one position.

// Find the second largest element in an array.

// Count the number of duplicate elements in an array.

// Remove duplicate elements from a sorted array.

// Level 3: Advanced / DSA-Oriented

// Find the majority element in an array (appearing more than n/2 times).

// Find the subarray with the maximum sum (Kadane’s Algorithm).

// Merge two sorted arrays into a single sorted array.

// Find common elements in two sorted arrays.

// Find a pair with given sum in an array.

// Count inversions in an array.

// Move all zeroes to the end of an array.

// Find the leader elements in an array (elements greater than all to the right).

// Implement 2D array input and output.

// Implement sparse matrix storage using arrays.

// Implement prefix sum array.

// Implement rotation check: check if one array is rotation of another.
}