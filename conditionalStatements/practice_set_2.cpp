// A. EASY LEVEL (1–30)

// Basic decision-making, simple conditions, numeric and character checks.

// Input a number and print if it is positive or negative.

// Input a number and print if it is even or odd.

// Input age and print "Adult" or "Minor".

// Input a character and check if it is a vowel or consonant.

// Input two numbers and print the greater one.

// Input two numbers and print the smaller one.

// Input three numbers and print the largest.

// Input marks and print "Pass" if marks > 33 else "Fail".

// Input a number and print if it is divisible by 5.

// Input a number and print if it is divisible by both 3 and 5.

// Input temperature and print “Hot” if > 30.

// Input year and check if it is a leap year.

// Input a char and check if it's alphabet or not.

// Input a char and check if it's uppercase or lowercase.

// Input three sides and check if triangle is valid.

// Input a number and check if it is a single-digit number.

// Input a number and check if it is a 3-digit number.

// Input a number and check if it lies between 1 and 100.

// Input the time in 24h and print “Good Morning/Eve/Night”.

// Input a number and check if it ends with digit 5.

// Input percentage and print "Grade A/B/C/D/F".

// Input salary and check if eligible for bonus (>50000).

// Input a number and check if it's prime or not (basic logic).

// Input two integers and check if both are even.

// Input two integers and print the maximum using if–else.

// Input a floating value and check if it has decimal part.

// Input char and check if it is digit or letter.

// Input day number (1–7) and print day name.

// Input month number and print number of days.

// Input a number and check if it is zero or non-zero.

// B. MEDIUM LEVEL (31–70)

// Nested if-else, multiple conditions, classification, pattern logic.

// Input three numbers and print second largest.

// Check if input age allows voting, driving, both, or none.

// Input sides and classify triangle: Equilateral/Isosceles/Scalene.

// Input a char and check if it is upper vowel, lower vowel, or consonant.

// Input marks and categorize into A+, A, B, C, D, Fail.

// Input number N, check: positive even, positive odd, negative even, negative odd.

// Input time in hours and print train fare category.

// Input electricity units and calculate bill with slabs.

// Input 3 numbers and print them in descending order (if–else only).

// Input a 4-digit number and check if it's an Armstrong number.

// Input integer and check if it's a palindrome (compare digits).

// Input a number and check if it's a perfect number.

// Input coordinates (x, y) and print quadrant.

// Input age and gender; decide insurance premium.

// Input speed and print “Slow/Normal/Fast/Overspeed”.

// Input two numbers and check if one is multiple of the other.

// Input a year and print century it belongs to.

// Input salary and print tax (0%, 5%, 20%, 30% based on slabs).

// Input number and classify as small/medium/large/very large.

// Input temperature and print category: Cold/Warm/Hot.

// Input integer N and print if digits are strictly increasing.

// Input 4 integers and print the smallest.

// Input a number and check if it’s a power of 2 (if-else only).

// Input three angles and check if triangle is acute/right/obtuse.

// Input two strings and check if they’re equal without using ==.

// Input BMI and print health category.

// Input day no. and check if weekend or weekday.

// Input a number and check if its digits contain 7.

// Input username and password and check login validity.

// Input basic salary and calculate HRA, DA, gross salary based on slabs.

// Input 5 subject marks and check if total >= required cutoff.

// Check if two given times (HH MM SS) are equal or not.

// Input a floating number and check if it is exactly integer (decimal == 0).

// Input roll number and classify: even → classroom A, odd → B.

// Input 3 numbers and check if they can form arithmetic progression.

// Input 3 numbers and check if they can form geometric progression.

// Input product quantity and calculate discount percentage slab.

// Input month number and print season (winter/summer/rain).

// Input number of days late and compute library fine.

// Input integer and check if sum of digits is even or odd.

// C. ADVANCED LEVEL (71–100)

// Complex nested conditions, simulation rules, multi-condition systems.

// Input time HH MM SS and check if it's a valid time.

// Input date DD MM YYYY and check validity including leap year.

// Simulate ATM: check balance, withdrawal limit, minimum balance.

// Simulate grading: if attendance < 75% → fail regardless of marks.

// Input coordinates and classify as origin/axis/quadrant.

// Input a 6-digit OTP and validate: must start with 1 or 2, last digit odd.

// Input a character and check if it's alphanumeric without using library.

// Input 4 numbers and classify them as all equal, two pairs, all different.

// Validate strong password: min length 8, contains digit, special char.

// Input salary and determine tax + surcharge rules.

// Validate IP address format (only using if–else).

// Check if a string is valid binary (only 0 & 1).

// Check if a number is Kaprekar number.

// Print type of browser based on user-agent substring match.

// Detect if a user is eligible for loan: income, age, credit score.

// Accept 3 numbers and classify them as increasing, decreasing, random.

// Student ranking logic: marks >90 → “A+”; marks >90 but attendance <60 → “B”.

// Given two intervals, check if they overlap.

// Check ISBN-10 validity using checksum logic (if–else).

// Validate triangle type including degenerate cases.

// If two integers are same parity (both even/odd) print “Same Type”.

// Simulate cafeteria menu based on time and day.

// Compare two times and print which one is earlier.

// Input a three-digit number and check if digits are non-decreasing.

// Validate a given 5-digit PIN (must not start with 0, must be numeric).

// Check if two strings are anagrams (no loops, use only if + recursion allowed).

// Simulate elevator logic: floor requested, current floor, direction.

// Validate email: must contain '@' and '.' in correct order.

// Check if a number is “near prime”: number+1 or number-1 prime.

// Multi-level age rule: child <10, teen <18, adult <60, senior >=60.